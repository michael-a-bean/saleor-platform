{
  "mcpServers": {
    "saleor-graphql": {
      "command": "docker",
      "args": [
        "run", "--rm", "-i",
        "--network", "host",
        "-e", "ENDPOINT_URL=http://localhost:8000/graphql/",
        "node:20-alpine",
        "npx", "-y", "mcp-graphql"
      ]
    }
  }
}
