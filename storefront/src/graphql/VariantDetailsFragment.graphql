fragment VariantDetails on ProductVariant {
	id
	name
	quantityAvailable
	pricing {
		price {
			gross {
				currency
				amount
			}
		}
	}
	attributes {
		attribute {
			slug
			name
		}
		values {
			slug
			name
		}
	}
}
